{% extends "show/show.html.twig" %}
{% block body %}

    <div class="jumbotron">
        <div class="container-fluid">
            <div class="row justify-content-md-center">
                <div class="mh-100 col-md-8 bg-white bg-light border  letter disable-select text-body p-5" style="min-height: 800px;">
                    {{ message | raw }}
                </div>
            </div>
        </div>
    </div>
    {{ encore_entry_script_tags('letter') }}
{% endblock %}